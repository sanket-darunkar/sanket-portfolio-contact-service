spring:
  application:
    name: contact-service

  # Mail — all credentials injected via environment variables, never hardcoded
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  # Render injects PORT; SERVER_PORT is kept as a secondary override for other platforms.
  port: ${PORT:${SERVER_PORT:8080}}

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# Logging — INFO in production; local profile overrides to DEBUG
logging:
  level:
    dev.sanket.contactservice: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# App-specific configuration (secrets injected via environment variables)
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  contact:
    recipient-email: ${CONTACT_RECIPIENT_EMAIL:}
